# Installation

The SDK is published on PyPI as `nivara`.

```
python3 -m pip install nivara
```

Supported Python versions: 3.8+

Optional editable install for contributors:
```
python3 -m pip install -e .
```

