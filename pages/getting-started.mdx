---
title: Getting Started
description: Install, configure, and send your first metric with Nivara.
---

# Getting Started

This guide shows how to install, configure, and send your first metric.

## Requirements

- Python 3.8+
- A Nivara API key: `ak_live_<id>.<secret>`

## Install

```bash
python3 -m pip install nivara
```

## Environment

```bash
export NIVARA_API_KEY="ak_live_..."            # required
export NIVARA_BASE_URL="https://api.getnivara.com"  # optional (default)
```

## Send your first metric (background emit)

```python title="real-code/examples/background_emit.py"
"""Background emit with flush and stats.

export NIVARA_API_KEY=ak_live_...
"""

import nivara as nv


def main() -> int:
    nv.configure(timeout=2.0, retries=1, mode="background")
    print(nv.emit(metric="llm.request", input_tokens=10, output_tokens=2))
    print("flushed:", nv.flush(timeout=2.0))
    print("stats:", nv.stats())
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

## Synchronous one‑shot

```python title="real-code/examples/sync_record.py"
"""Synchronous record.

export NIVARA_API_KEY=ak_live_...
"""

from datetime import datetime, timezone
import nivara as nv


def main() -> int:
    res = nv.record(
        metric="llm.request",
        ts=datetime.now(timezone.utc),
        input_tokens=5,
        output_tokens=1,
    )
    print(res)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```
